# MicroGrid AI — Backend dependencies
# Versions are flexible ranges to avoid ABI conflicts on Railway (Python 3.12)

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Database
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
pydantic-settings>=2.5.0

# Auth + security
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
slowapi>=0.1.9

# Cache
redis[hiredis]>=5.0.0

# AI / ML
xgboost>=2.1.0
scikit-learn>=1.5.0
numpy>=2.0.0,<2.3
pandas>=2.2.0
pvlib>=0.11.0

# Optimizer (LP solver — must match numpy ABI)
scipy>=1.15.0,<1.16

# Background jobs
apscheduler>=3.10.0

# HTTP client
httpx>=0.27.0
requests>=2.32.0

# WhatsApp alerts
twilio>=9.0.0

# PDF report generation
reportlab>=4.0.0

# Observability
sentry-sdk[fastapi]>=2.0.0

# Supabase client
supabase>=2.7.0

# Utilities
python-dateutil>=2.9.0
pytz>=2024.1
email-validator>=2.2.0

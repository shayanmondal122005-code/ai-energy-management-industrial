# Let Nixpacks auto-detect Python from requirements.txt
# Use python -m uvicorn so it works even if uvicorn isn't on PATH

[start]
cmd = "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"

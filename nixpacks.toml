# Nixpacks config for Railway

# xgboost needs OpenMP runtime (libgomp) — not in minimal Python image.
# Without it, `import xgboost` crashes the app on startup.
[phases.setup]
aptPkgs = ["libgomp1"]

[start]
cmd = "python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1"
